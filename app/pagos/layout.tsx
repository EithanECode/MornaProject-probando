"use client";

// Layout para rol pagos (validador de pagos global)
import React from 'react';

export default function PagosLayout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</>
  );
}
